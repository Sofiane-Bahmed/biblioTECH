$kind: http-request
name: Reset Password
method: PATCH
url: '{{baseUrl}}/auth/reset-password/{{resetToken}}'
order: 6000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "password": "newpassword123"
    }
